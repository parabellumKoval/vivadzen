# Profile To‑Do

1. **Fix schema & guard mismatches.** `App/Http/Controllers/Api/ProfileController::update` authenticates via `auth:sanctum` but fetches the user through the `profile` guard (`packages/profile/src/app/Http/Controllers/Api/ProfileController.php`), and `packages/profile/IMPROVEMENTS.md` calls out `firstname`/`lastname` vs. `first_name` columns. Pick a single guard, align migrations/models (`src/database/migrations/2025_09_07_000000_create_ak_profiles_table.php` maybe), and add regression tests so profile APIs work for SPA clients.
2. **Stabilize admin settings.** `SettingsCrudController::store` references translator-specific classes that do not exist yet, and the publicly published view (`resources/views/settings.blade.php`) renders an empty form. Either complete the settings persistence or temporarily hide the UI until the controller is ready.
3. **Add missing migrations/documents.** `SocialAccount` expects `ak_social_accounts` yet no migration ships, and `captures`/`referral clicks` tables referenced in middleware are also missing. Publish the required migrations or make logging optional so the referral event loop can run on clean installs.
4. **Document wallet contracts.** The `Wallet` contract signature differs from `WalletService` (`packages/profile/IMPROVEMENTS.md` highlights the drift). Decide on a fixed interface (with currency) and update bindings to prevent fatal errors when the wallet ledger is consumed outside the package.
