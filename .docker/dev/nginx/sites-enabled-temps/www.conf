server {
    listen 80;

    # WWW subdomain
    server_name www.front.localhost;

    # Security headers
    include /etc/nginx/snippets/security.conf;

    # Redirect to non-www domain
    return 301 http://example.local;
}